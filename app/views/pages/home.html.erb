<div class="global  center tc">
  <div class="dt dt--fixed">
    <div class="dtc tc pv4 h5">
      <% if current_user %>
          <div class="personal 	2.25rem">Здравствуйте, <%= current_user.name %>
            <br>
            <%= image_tag(current_user.image, :class => "personalimg")%>
          </div>
          <div class="friends">
            <% @vk = VkontakteApi::Client.new %>
            <% @friends = @vk.friends.get(user_ids: current_user.uid, count: "50", access_token: current_user.token , v: "5.1", fields: ["photo_50"]) %>
            <% @friends.items.sample(5).each do |friend| %>
                <% first_name = friend.first_name %>
                <% last_name = friend.last_name %>
                <% image = friend.photo_50 %>
                <div class="friend tc center">
                  <span class="name 1.5rem"><%= first_name %></span>
                  <span class="surname"><%= last_name %></span>
                  <%= image_tag(image, :class => "friendimg") %>
                </div>
            <% end %>
          </div>
      <% end %>
    </div>
  </div>
  <% unless current_user %>
      <span><%= link_to "VKontakte", user_vkontakte_omniauth_authorize_path, :class => "f6 login link dim ba mb2 dib navy" %></span>
  <% else %>
      <span><%= link_to "Выход", destroy_user_session_path, method: :delete, :class => "f6 login link dim ba mb2 dib navy"  %></span>
  <% end %>
</div>

